# 2026-04-30 Project Memory

> Auto-generated by AI Agent. Entries are append-only within a day.
> Track: lark-island。本日仅 1 条 m9 hotfix（commit 时间 00:03，是昨晚 m9 实测的延续）；其余实测、commit 都见 [[2026-04-29]]。

---

## [00:03] M9 hotfix: ACTION SYNTAX guard 必须放进 BASE_SYSTEM_PROMPT，不止 skill addendum

**Type**: correction
**Tags**: `bug`, `prompt`, `vlm`, `m9-hotfix`, `runner`
**Related**: commit `9de2816`；[[2026-04-29#20:27]] m7-hotfix-doc-calendar；[[2026-04-29#22:03]] m9-feat

M9 verify-run #4：用户在飞书 IM 给 Lark-island 机器人发"hello bot, what is 1+1"3 次。第 1 次任务 completed，第 2/3 次都 ❌ pageError `Too many action execute failures: Missing startX(249.6) or startY249.6 / startX(34.56) or startY34.56`。

灵动岛 thought 显示 VLM 都已经知道答案是 2 想直接 finished()，但**输出的 click action 偶发漂到无引号格式** → BrowserOperator 解析 startY 为 null → throw → retry 3 次 → fail。**老熟人 bug**（[[2026-04-29#20:27]] m7-hotfix 在 doc Run 1 撞过同款）。

根因：m7 hotfix 给 doc/calendar 加了 ACTION SYNTAX 段，m8 给 base/mail 加了，但 **generic 模式（无 skill 命中）+ feishu_im_send 仍然只靠 BASE_SYSTEM_PROMPT 默认那行 `click(start_box='[x1, y1, x2, y2]')` 展示**——展示了正确格式但没显式 WRONG/CORRECT 对照，VLM 偶发漂仍存在。

m9 让 generic 高频被触发（飞书 IM 简单 prompt 不命中飞书 skill 关键词，全 fallback 到 generic），所以同一 bug 从"rare doc/calendar 问题"变成"每次 hello bot 一半概率失败"。

修：把 ACTION SYNTAX — STRICT 段加到 `runners/web-agent/src/agent/runtime.ts` 的 `BASE_SYSTEM_PROMPT`，generic 模式 + 5 个 skill 一次覆盖。各 skill 的 systemPromptAddendum 仍保留自己的 ACTION SYNTAX 段——重复无害且强化遵循。

`runtime.test.ts` 现有 generic-mode 测试加 4 个 expect：BASE prompt 含 `ACTION SYNTAX — STRICT` / `click(start_box='[` / `WRONG` / `hotkey(key='escape')`。87/87 仍 pass。

**为什么 m7/m8 没把这步直接做对**：m7 hotfix 时只看到 doc 一个 skill 受影响就只改了它，没意识到 generic 模式同样裸奔。m9 让 generic 频繁被触发后 bug 才暴露。**教训**：发现 prompt drift 时，先看哪一层是公共部分（BASE prompt）再决定补哪一层；只补 leaf skill 是 sub-optimal。

**未验证**：实际 verify-run #5（用户在飞书 IM 连发 3 次 "hello bot, what is 1+1" 看 3/3 是否都拿到 finalAnswer）尚未跑。如果仍偶发漂，说明 prompt 工程已穷尽，需启动 path B（DOM `getByRole/getByText`，m6 §8.1 备选）。

**Status**: ⏳ pending verification（待 verify-run #5）

---

## Pattern: VLM 坐标格式漂移是 BASE prompt 级 issue，不是 skill 级 issue

**Type**: optimization
**Tags**: `pattern`, `prompt`, `vlm`

四次实测累计踩到同款 bug：

1. M7 doc Run 1（[[2026-04-29#20:27]]）：`click(start_box=[265,100,370,160])`
2. M9 verify-run #4 generic mode（本日 [[2026-04-30#00:03]]）：`startX(249.6)` / `startX(34.56)`
3. （历史）M6 第一次 IM 实测里 `hotkey(key='esc')`——同类（key 名漂） 

`@ui-tars/sdk` BrowserOperator 在 action 解析失败时 throw 模板有自身 bug（`Missing startX(...) or startY...` 里 startY 重用 startX），但**真因是 VLM 输出**。Qwen3-VL-Plus 在 `## Action Space` 仅展示一行 canonical 格式时偶发漂——加 WRONG/CORRECT 对照例子能确定性消除。

**Resolved Pattern**：
- 凡 prompt drift 类 bug，先把 guard 加到**最浅的公共 prompt 层**（BASE_SYSTEM_PROMPT），而不是只贴单个 skill 的 systemPromptAddendum
- WRONG/CORRECT 对照例子比单展示 CORRECT 强得多——VLM 看见反例会避免漂

加进 [[project-overview#Resolved Patterns]]。

**Status**: ✅ verified（pattern 跨多次实测稳定复现）
