# CUA-Lark OPC Execution Plan

版本：`v3.0`

日期：`2026-04-28`

目标截止：`2026-05-07`

状态：`六子产品覆盖 + demo 优先执行排期`

## 1. 本阶段唯一目标

`2026-05-07` 前，全队唯一目标不是包装，而是：

1. 每个人把自己负责的子产品 demo 跑出来
2. 至少跑出多条跨子产品联动 demo
3. 把最终主工作流整合出来

所以本阶段：

1. 不以 PPT 为先
2. 不以长文档材料为先
3. 以主链路稳定和 demo 跑通为先

## 2. 子产品覆盖分配

## 2.1 王泽

负责：

1. `IM`
2. `Calendar`

## 2.2 董圣娇

负责：

1. `Docs`
2. `VC`

## 2.3 钟梓文

负责：

1. `Base`
2. `Mail`
3. 六子产品联动测试
4. 最终所有工作流整合
5. 灵动岛 / 主 UI

## 3. 每个人的阶段目标

## 3.1 王泽

阶段目标：

1. `IM` 最小 demo
2. `Calendar` 最小 demo
3. `IM` 多步链
4. `Calendar` 多步链
5. `IM -> Calendar` 联动尝试
6. `异常场景处理`
7. `自愈式执行`
8. `多轮对话编排`

## 3.2 董圣娇

阶段目标：

1. `Docs` 最小 demo
2. `VC` 最小 demo
3. `Docs` 多步链
4. `VC` 多步链
5. `Docs -> VC` 或 `Calendar -> Docs` 联动尝试
6. `测试用例自动生成`
7. `混合定位策略`
8. `录制回放`

## 3.3 钟梓文

阶段目标：

1. `Base` 最小 demo
2. `Mail` 最小 demo
3. `Base` 多步链
4. `Mail` 多步链
5. 六子产品联动测试框架
6. 最终工作流整合
7. 灵动岛 / UI 可展示版
8. 六子产品联动测试实际落地
9. 所有工作流最终串联
10. 进阶能力 UI 呈现

## 4. 时间推进

## 4.1 4月28日 - 4月29日

目标：

**每人先把 2 个子产品的最小 demo 跑起来**

王泽：

1. `IM` 最小操作链
2. `Calendar` 最小操作链

董圣娇：

1. `Docs` 最小操作链
2. `VC` 最小操作链

钟梓文：

1. `Base` 最小操作链
2. `Mail` 最小操作链
3. 灵动岛和主 UI 骨架

验收：

1. 六个子产品至少都有最小 demo 定义

## 4.2 4月30日 - 5月2日

目标：

**把各自主链补成多步工作流**

王泽：

1. `IM` 搜索 -> 输入 -> 发送 -> 验证
2. `Calendar` 创建 -> 邀请 -> 修改 -> 查看
3. `IM -> Calendar` 联动尝试
4. 异常场景识别与恢复
5. 自愈替代路径尝试

董圣娇：

1. `Docs` 创建 -> 编辑 -> 结构化写入
2. `VC` 发起 -> 加入 -> 基础控制
3. `Docs -> VC` 或 `Calendar -> Docs` 联动尝试
4. 录制回放草案
5. 混合定位策略验证

钟梓文：

1. `Base` 建表 -> 字段 -> 录入 -> 视图
2. `Mail` 写信 -> 附件 -> 发送 -> 回复
3. 起六子产品联动测试框架
4. 起 UI 的运行态展示
5. 联动测试脚手架
6. 最终整合 UI 结构

验收：

1. 每人负责的两个子产品都至少有一条多步链

## 4.3 5月3日 - 5月5日

目标：

**进入联动与整合阶段**

王泽：

1. 修 `IM / Calendar` 稳定性
2. 配合主工作流整合
3. 收口对应自然语言入口
4. 收口异常场景处理
5. 收口自愈式执行和多轮编排

董圣娇：

1. 修 `Docs / VC` 稳定性
2. 配合主工作流整合
3. 做回归和问题记录
4. 收口测试用例自动生成
5. 收口混合定位和录制回放

钟梓文：

1. 跑六子产品联动测试
2. 串最终工作流
3. 完成灵动岛 / UI 可展示版
4. 完成六子产品联动验证结果
5. 完成最终 demo 整合和 UI 呈现

验收：

1. 至少形成两条联动链
2. 开始有最终整合 demo

## 4.4 5月6日 - 5月7日

目标：

**只收口 demo**

王泽：

1. 只修 `IM / Calendar`
2. 反复跑 demo

董圣娇：

1. 只修 `Docs / VC`
2. 反复跑 demo

钟梓文：

1. 只修 `Base / Mail / 联动 / UI`
2. 跑最终整合 demo

验收：

1. 六子产品 demo 基本可展示
2. 联动 demo 可展示
3. 最终工作流可展示

## 5. 当前建议的主工作流

本阶段最值得整合的主工作流建议是：

1. `IM -> Calendar -> Docs`
2. `Calendar -> Docs -> VC`
3. `Docs / Base -> Mail`

这样能同时体现：

1. 多产品联动
2. 基础操作能力
3. 进阶联动能力

## 5.1 当前建议的 3.3 进阶能力责任归属

王泽：

1. `异常场景处理`
2. `自愈式执行`
3. `多轮对话编排`

董圣娇：

1. `测试用例自动生成`
2. `混合定位策略`
3. `录制回放`

钟梓文：

1. `六个子产品联动测试`
2. `最后所有工作流整合`
3. `主 UI / 灵动岛整合展示`

## 6. 本阶段明确不优先

不优先：

1. PPT
2. 长文档包装
3. 非 demo 导向的额外需求

只优先：

1. 子产品主链
2. 联动链
3. 最终工作流
4. UI 展示壳

## 7. 最终判断

按现在这版分工，最合理的打法就是：

1. `angla` 打透 `IM + Calendar`
2. `董圣娇` 打透 `Docs + VC`
3. `钟梓文` 打透 `Base + Mail + 联动整合 + UI`

这样到 `5.7`，团队最有可能拿出来的是：

1. 六个子产品都有 demo
2. 至少两到三条联动链
3. 一个完整、可展示的最终主工作流
