# CUA-Lark Team Roles

版本：`v3.0`

日期：`2026-04-28`

状态：`5.7 前以 demo 主链跑通为优先的正式分工`

## 1. 当前总原则

`2026-05-07` 前，团队最优先的事不是：

1. PPT
2. 长文档材料
3. 答辩包装

而是：

1. 把各自负责的子产品主链跑通
2. 把多子产品联动跑通
3. 把最终 demo 主工作流整合出来

所以本阶段所有分工都围绕：

**谁负责哪几个子产品，谁负责联动整合，谁负责 UI 展示壳，最终如何把 demo 跑出来。**

## 2. 子产品责任划分

本期按 `6` 个飞书子产品拆分：

1. `IM`
2. `Calendar`
3. `Docs`
4. `VC`
5. `Base`
6. `Mail`

## 2.1 王泽

负责子产品：

1. `IM`
2. `Calendar`

## 2.2 董圣娇

负责子产品：

1. `Docs`
2. `VC`

## 2.3 钟梓文

负责子产品：

1. `Base`
2. `Mail`

并额外负责：

1. `6` 个子产品的联动测试
2. 最终所有工作流整合
3. 灵动岛 / 主 UI 壳

## 3. 详细职责

## 3.1 王泽：IM + Calendar 主链负责人

核心职责：

1. 跑通 `IM` 的基础操作和多步链路
2. 跑通 `Calendar` 的基础操作和多步链路
3. 负责 `IM + Calendar` 相关自然语言场景拆解
4. 负责这两条主链的异常处理与稳定性优化
5. 负责这两条主链的 demo 路径设计

必须交付：

1. `IM` 可演示 demo
2. `Calendar` 可演示 demo
3. 至少 `1` 条围绕 `IM / Calendar` 的联动链

5.7 前额外承担的 3 个明确任务：

1. `IM` 搜索 / 发送 / 验证主链
2. `Calendar` 建会 / 邀请 / 修改主链
3. 围绕这两条链的场景与异常策略收口

对应承担的 `3.3` 进阶能力：

1. `异常场景处理`
2. `自愈式执行`
3. `多轮对话编排`

本阶段不优先承担：

1. PPT 制作
2. 最终 UI 主实现
3. 六子产品总联调主负责人

## 3.2 董圣娇：Docs + VC 主链负责人

核心职责：

1. 跑通 `Docs` 的基础操作和多步链路
2. 跑通 `VC` 的基础操作和多步链路
3. 负责这两条主链的基础功能测试和回归记录
4. 负责 `Docs / VC` 相关联动场景验证

必须交付：

1. `Docs` 可演示 demo
2. `VC` 可演示 demo
3. 至少 `1` 条围绕 `Docs / VC` 的联动链

5.7 前额外承担的 3 个明确任务：

1. `Docs` 创建 / 编辑 / 结构化写入主链
2. `VC` 发起 / 加入 / 基础控制主链
3. 这两条主链的回归测试与问题记录

对应承担的 `3.3` 进阶能力：

1. `测试用例自动生成`
2. `混合定位策略`
3. `录制回放`

本阶段不优先承担：

1. PPT 制作
2. 灵动岛主实现
3. 总体产品定位拍板

## 3.3 钟梓文：Base + Mail 主链负责人 / 联动整合负责人 / UI 负责人

核心职责：

1. 跑通 `Base` 的基础操作和多步链路
2. 跑通 `Mail` 的基础操作和多步链路
3. 负责 `6` 个子产品的联动测试
4. 负责最终所有工作流整合
5. 负责灵动岛和主 UI 壳
6. 负责把各人产出的子产品链路接成最终 demo

必须交付：

1. `Base` 可演示 demo
2. `Mail` 可演示 demo
3. 六子产品联动测试结果
4. 最终 demo 整合版本
5. 灵动岛 / 主 UI 可展示版

5.7 前额外承担的 3 个明确任务：

1. `Base` 建表 / 字段 / 录入 / 视图主链
2. `Mail` 写信 / 附件 / 发送 / 回复主链
3. 六子产品整合、联动测试和最终 UI 收口

对应承担的 `3.3` 进阶职责：

1. `六个子产品的联动测试`
2. `最后所有工作流整合`
3. `主 UI / 灵动岛整合展示`

本阶段不优先承担：

1. PRD 主撰写
2. 长文档材料主收口

## 4. 六子产品建议场景对齐

## 4.1 王泽

### IM

建议优先：

1. 搜索消息记录
2. 搜索联系人
3. 发送文本消息
4. 验证发送成功

### Calendar

建议优先：

1. 创建日程
2. 邀请参会人
3. 修改时间
4. 查看忙闲状态

## 4.2 董圣娇

### Docs

建议优先：

1. 创建文档
2. 编辑正文
3. 插入标题 / 列表
4. 分享或同步结果

### VC

建议优先：

1. 发起会议
2. 加入会议
3. 开关麦克风 / 摄像头

## 4.3 钟梓文

### Base

建议优先：

1. 创建表格
2. 添加字段
3. 录入数据
4. 切换视图

### Mail

建议优先：

1. 撰写邮件
2. 添加附件
3. 发送邮件
4. 回复邮件

## 5. 联动和整合分工

## 5.1 第一层：各自先跑通本产品主链

顺序：

1. 王泽跑通 `IM + Calendar`
2. 董圣娇跑通 `Docs + VC`
3. 钟梓文跑通 `Base + Mail`

## 5.2 第二层：两两联动

建议优先联动：

1. `IM -> Calendar`
2. `Calendar -> Docs`
3. `Docs -> VC`

## 5.3 第三层：钟梓文负责六子产品联动测试和最终整合

钟梓文在本阶段额外承担：

1. 联动测试计划
2. 六子产品整合验证
3. 统一 UI 展示壳
4. 最终 demo 主工作流串联

这部分同时对应比赛 `3.3` 的加分项收口：

1. `跨产品联动测试`
2. `整合后的多轮执行展示`
3. `进阶能力的最终可视化呈现`

## 6. 到 5.7 前的任务节奏

## 6.1 4月28日 - 4月29日

目标：

**每个人先把自己负责的 2 个子产品拆成最小可跑 demo**

王泽：

1. `IM` 最小 demo
2. `Calendar` 最小 demo

董圣娇：

1. `Docs` 最小 demo
2. `VC` 最小 demo

钟梓文：

1. `Base` 最小 demo
2. `Mail` 最小 demo
3. 起灵动岛和 UI 骨架

## 6.2 4月30日 - 5月2日

目标：

**每个人把自己两条主链补成多步工作流**

王泽：

1. `IM` 多步链
2. `Calendar` 多步链
3. `IM -> Calendar` 试联动

董圣娇：

1. `Docs` 多步链
2. `VC` 多步链
3. `Calendar -> Docs` 或 `Docs -> VC` 试联动

钟梓文：

1. `Base` 多步链
2. `Mail` 多步链
3. 六子产品联动测试框架
4. UI 展示壳继续推进

## 6.3 5月3日 - 5月5日

目标：

**开始整合主工作流并集中修 demo**

王泽：

1. 修 `IM / Calendar` 稳定性
2. 配合最终工作流串联
3. 收口自己负责场景的自然语言入口

董圣娇：

1. 修 `Docs / VC` 稳定性
2. 配合最终工作流串联
3. 回归测试并记录阻塞

钟梓文：

1. 六子产品联动测试
2. 最终工作流整合
3. 灵动岛 / UI 可展示版

## 6.4 5月6日 - 5月7日

目标：

**只收口 demo，不扩其他材料**

王泽：

1. 只修 `IM / Calendar`
2. 跑 demo

董圣娇：

1. 只修 `Docs / VC`
2. 跑 demo

钟梓文：

1. 只修 `Base / Mail / 联动 / UI`
2. 跑最终 demo

## 7. 5.7 前明确不优先做的事

本阶段不作为首要事项：

1. PPT
2. 长文档答辩材料
3. 大量文字包装

本阶段首要事项只有：

1. 每个人的子产品 demo
2. 多子产品联动 demo
3. 最终整合 demo

## 8. 最终判断

现在的最优策略已经不是“谁更会写材料”，而是：

1. 王泽把 `IM + Calendar` 打透
2. 圣娇把 `Docs + VC` 打透
3. 钟梓文把 `Base + Mail + 联动整合 + UI` 打透

这样到 `5.7`，团队最有机会拿出来的是：

1. 六个子产品都有 demo
2. 至少两到三条联动链
3. 一个真正完整的主工作流演示
