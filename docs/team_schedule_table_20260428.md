# CUA-Lark Team Schedule Table

版本：`v1.0`

日期：`2026-04-28`

状态：`分工 + 排期一张表`

## 总原则

`2026-05-07` 前只优先四件事：

1. 各自子产品 demo 跑通
2. 进阶能力 `3.3` 落到人
3. 联动链跑通
4. 最终整合 demo 跑通

PPT、长文档包装、答辩美化都不是当前第一优先级。

## 分工总表

| 成员 | 主负责子产品 | 5.7 前主目标 | 对应 3.3 进阶能力 / 职责 | 明确交付 |
| --- | --- | --- | --- | --- |
| 王泽 | `IM`、`Calendar` | 跑通 `IM + Calendar` 主链并形成至少 `1` 条联动链 | `异常场景处理`、`自愈式执行`、`多轮对话编排` | `IM` demo、`Calendar` demo、`IM -> Calendar` 联动 demo |
| 董圣娇 | `Docs`、`VC` | 跑通 `Docs + VC` 主链并形成至少 `1` 条联动链 | `测试用例自动生成`、`混合定位策略`、`录制回放` | `Docs` demo、`VC` demo、`Docs -> VC` 或 `Calendar -> Docs` 联动 demo |
| 钟梓文 | `Base`、`Mail` | 跑通 `Base + Mail` 主链，并负责六子产品联动测试、最终工作流整合和主 UI | `六个子产品联动测试`、`最后所有工作流整合`、`主 UI / 灵动岛整合展示` | `Base` demo、`Mail` demo、联动测试结果、最终整合 demo、灵动岛 / UI 展示版 |

## 子产品细化表

| 成员 | 子产品 | 建议优先功能 | 5.7 前最低验收 |
| --- | --- | --- | --- |
| 王泽 | `IM` | 搜索消息记录、搜索联系人、发送文本消息、验证发送成功 | 至少跑通 `搜索 -> 输入 -> 发送 -> 验证` |
| 王泽 | `Calendar` | 创建日程、邀请参会人、修改时间、查看忙闲状态 | 至少跑通 `创建 -> 邀请 -> 修改 / 查看` |
| 董圣娇 | `Docs` | 创建文档、编辑正文、插入标题 / 列表、分享或同步结果 | 至少跑通 `创建 -> 编辑 -> 结构化写入` |
| 董圣娇 | `VC` | 发起会议、加入会议、开关麦克风 / 摄像头 | 至少跑通 `发起 / 加入 -> 基础控制` |
| 钟梓文 | `Base` | 创建表格、添加字段、录入数据、切换视图 | 至少跑通 `建表 -> 字段 -> 录入 -> 视图` |
| 钟梓文 | `Mail` | 撰写邮件、添加附件、发送邮件、回复邮件 | 至少跑通 `写信 -> 附件 -> 发送 / 回复` |

## 3.3 进阶能力分配表

| 进阶能力 | 负责人 | 负责范围 | 5.7 前验收方式 |
| --- | --- | --- | --- |
| 异常场景处理 | 王泽 | `IM + Calendar` 中的弹窗、失焦、加载超时、权限提示 | 至少 `1` 条异常恢复链路能演示 |
| 自愈式执行 | 王泽 | `IM + Calendar` 的失败替代路径与重试策略 | 至少 `1` 个失败后恢复案例 |
| 多轮对话编排 | 王泽 | `IM -> Calendar` 任务拆解与动态后续步骤 | 至少 `1` 条多轮任务 demo |
| 测试用例自动生成 | 董圣娇 | `Docs + VC` 的结构化 case 草稿生成 | 至少输出 `1` 份自动生成 case 示例 |
| 混合定位策略 | 董圣娇 | `Docs + VC` 的视觉 + DOM / Accessibility 融合定位 | 至少 `1` 条链路使用混合定位 |
| 录制回放 | 董圣娇 | `Docs + VC` 演示路径的录制与复用 | 至少 `1` 条可回放路径 |
| 六个子产品联动测试 | 钟梓文 | 所有子产品联动验证 | 形成联动测试表和通过 / 失败结果 |
| 最后所有工作流整合 | 钟梓文 | 最终整合 demo 主链 | 跑通至少 `1` 条最终整合主工作流 |
| 主 UI / 灵动岛整合展示 | 钟梓文 | 灵动岛、运行态、结果态、总 UI 收口 | 有可演示 UI 壳 |

## 排期总表

| 时间 | 王泽 | 董圣娇 | 钟梓文 | 阶段验收 |
| --- | --- | --- | --- | --- |
| `4/28 - 4/29` | 跑 `IM` 最小 demo；跑 `Calendar` 最小 demo | 跑 `Docs` 最小 demo；跑 `VC` 最小 demo | 跑 `Base` 最小 demo；跑 `Mail` 最小 demo；起灵动岛和主 UI 骨架 | 六个子产品至少都有最小 demo 定义 |
| `4/30 - 5/2` | 做 `IM` 多步链；做 `Calendar` 多步链；尝试 `IM -> Calendar`；做异常处理和自愈草案 | 做 `Docs` 多步链；做 `VC` 多步链；尝试 `Docs -> VC` 或 `Calendar -> Docs`；做录制回放和混合定位 | 做 `Base` 多步链；做 `Mail` 多步链；起六子产品联动测试框架；起运行态 UI | 每人负责的两个子产品都至少有一条多步链 |
| `5/3 - 5/5` | 修 `IM / Calendar` 稳定性；配合主工作流整合；收口异常处理 / 自愈 / 多轮编排 | 修 `Docs / VC` 稳定性；配合主工作流整合；收口测试用例自动生成 / 混合定位 / 录制回放 | 跑六子产品联动测试；串最终工作流；完成灵动岛 / UI 展示版 | 至少形成两条联动链，并出现最终整合 demo |
| `5/6 - 5/7` | 只修 `IM / Calendar`；反复跑 demo | 只修 `Docs / VC`；反复跑 demo | 只修 `Base / Mail / 联动 / UI`；跑最终整合 demo | 六子产品 demo 基本可展示，联动 demo 可展示，最终工作流可展示 |

## 当前建议的主工作流表

| 工作流 | 主负责 | 协同 | 目标 |
| --- | --- | --- | --- |
| `IM -> Calendar -> Docs` | 王泽 | 董圣娇、钟梓文 | 体现沟通到建会到文档沉淀 |
| `Calendar -> Docs -> VC` | 董圣娇 | 王泽、钟梓文 | 体现会议到文档到视频会议联动 |
| `Docs / Base -> Mail` | 钟梓文 | 王泽、董圣娇 | 体现信息沉淀到结构化记录再到邮件触达 |

## 5.7 前明确不优先

| 不优先事项 | 原因 |
| --- | --- |
| PPT | 当前最重要的是 demo 跑通，不是包装 |
| 长文档答辩材料 | 可以在 `5/7` 后再收口 |
| 非 demo 导向扩展功能 | 会分散六子产品主链推进速度 |

## 一句话收口

| 成员 | 一句话目标 |
| --- | --- |
| 王泽 | 打透 `IM + Calendar`，把沟通和建会链做稳 |
| 董圣娇 | 打透 `Docs + VC`，把内容和会议联动做稳 |
| 钟梓文 | 打透 `Base + Mail`，并把六子产品和 UI 最终整合出来 |
