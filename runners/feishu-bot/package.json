{
  "name": "@lark-island/feishu-bot",
  "version": "0.1.0-spike",
  "description": "Feishu IM bot bridge for lark-island. Spawns lark-cli `event +subscribe` to listen for IM messages, dispatches them as runWebAgentTask via BridgeServer observer role, replies via lark-cli `im +messages-send`.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
